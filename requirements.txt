# PhantomVox dependencies

# UI framework
flet==0.80.5

# TTS engine
# qwen-tts>=0.0.5  # Using local build instead (qwen_tts-0.0.6-py3-none-any.whl)
modelscope>=1.34.0

# Torch compilation dependencies (Windows only for torch.compile)
triton-windows<3.6; sys_platform == 'win32' and platform_machine == 'AMD64'

# Audio processing
sounddevice>=0.5.5
soundfile>=0.12.0
pydub>=0.25.1

# Other dependencies
aiohttp>=3.8.0
requests>=2.28.0
numpy>=1.24.0
psutil>=7.2.2
nvidia-ml-py>=13.590.48

# FastAPI (HTTP server)
fastapi>=0.128.0
uvicorn>=0.40.0
pydantic>=2.12.5
python-multipart>=0.0.22
